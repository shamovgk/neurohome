
# NeuroHome - IoT система для умного ухода за растениями 🌱

Полнофункциональная IoT система для мониторинга и автоматического ухода за растениями с использованием ESP32, React Native и Node.js backend.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Запуск проекта](#запуск-проекта)
- [Демо аккаунт](#демо-аккаунт)
- [Структура проекта](#структура-проекта)
- [API документация](#api-документация)
- [Разработка](#разработка)
- [Troubleshooting](#troubleshooting)

## 🚀 Технологический стек

### Backend
- **Node.js** + **Express** + **TypeScript**
- **PostgreSQL** - реляционная БД (пользователи, устройства)
- **MongoDB** - time-series данные датчиков
- **MQTT** (Mosquitto) - real-time коммуникация с ESP32
- **Socket.IO** - WebSocket для мобильного приложения

### Mobile
- **React Native** + **Expo**
- **Redux Toolkit** - state management
- **React Navigation** - маршрутизация
- **Socket.IO Client** - real-time обновления

### Hardware
- **ESP32** - микроконтроллер
- Датчики влажности почвы, температуры, освещенности
- Реле для управления поливом и освещением

## 📦 Требования

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **Docker** и **Docker Compose**
- **Git**
- **Expo Go** (для физического устройства)
- **iOS Simulator** (macOS) или **Android Studio** (для эмулятора)

## ⚡ Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/shamovgk/neurohome.git
cd neurohome
```

### 2. Установка зависимостей

```bash
# Установка зависимостей для всех пакетов
npm install
```

### 3. Настройка окружения

Создайте .env файлы:
```
    cp .env.example .env
    cp packages/backend/.env.example packages/backend/.env
    cp packages/mobile/.env.example packages/mobile/.env
```

### 4. Запуск всех сервисов

```bash
# Запуск Docker контейнеров (PostgreSQL, MongoDB, MQTT, Backend)
npm run dev
```

Это запустит:
- **Backend API**: http://localhost:3000
- **PostgreSQL**: localhost:5432
- **MongoDB**: localhost:27017
- **MQTT Broker**: localhost:1883
- **pgAdmin**: http://localhost:5050
- **Mongo Express**: http://localhost:8081

### 5. Создание демо данных

В отдельном терминале:

```bash
# Создание демо пользователя и устройства с тестовыми данными
docker exec -it neurohome-backend npm run seed:demo
```

Вы увидите:
```
Demo data seeding completed!

Demo credentials:
Email: demo@neurohome.com
Password: demo123
```

### 6. Запуск симулятора демо устройства (опционально)

Для real-time обновлений данных:

```bash
docker exec -it neurohome-backend npm run demo:simulator
```

Симулятор будет отправлять данные каждые 10 секунд.

### 7. Запуск мобильного приложения

В новом терминале:

```bash
cd packages/mobile
npm start
```

Выберите платформу:
- **i** - iOS Simulator (только macOS)
- **a** - Android Emulator
- **w** - Web браузер

## 🎭 Демо аккаунт

Для демонстрации и тестирования используйте:

### Учетные данные
- **Email**: `demo@neurohome.com`
- **Password**: `demo123`

### Демо устройство
- **Название**: Демо Фикус
- **Тип**: Растение
- **Местоположение**: Гостиная (демо)
- **Статус**: Online (если запущен симулятор)

## 📁 Структура проекта

```
neurohome/
├── docker/                      # Docker конфигурации
│   ├── backend.Dockerfile
│   └── mosquitto/
│       └── mosquitto.conf
├── packages/
│   ├── backend/                 # Node.js Backend
│   │   ├── src/
│   │   │   ├── config/         # Конфигурация БД, env
│   │   │   ├── controllers/    # API контроллеры
│   │   │   ├── models/         # ORM модели (Sequelize, Mongoose)
│   │   │   ├── routes/         # Express маршруты
│   │   │   ├── services/       # MQTT, Socket.IO сервисы
│   │   │   ├── middleware/     # Аутентификация, error handling
│   │   │   ├── utils/          # Утилиты (JWT, logger)
│   │   │   ├── scripts/        # Seed скрипты
│   │   │   └── server.ts       # Точка входа
│   │   └── package.json
│   └── mobile/                  # React Native приложение
│       ├── app/                # Expo Router страницы
│       │   ├── (auth)/         # Экраны аутентификации
│       │   └── (tabs)/         # Основные табы
│       ├── src/
│       │   ├── components/     # Переиспользуемые компоненты
│       │   ├── features/       # Фичи (auth, devices)
│       │   ├── services/       # API клиенты, WebSocket
│       │   ├── store/          # Redux store
│       │   ├── constants/      # Константы, темы
│       │   └── types/          # TypeScript типы
│       └── package.json
├── docker-compose.dev.yml       # Docker Compose для разработки
├── package.json                 # Root package.json
└── README.md
```

## 🔌 API документация

### Базовый URL
```
http://localhost:3000/api/v1
```

### Endpoints

#### Аутентификация

**POST** `/auth/register`
```json
{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "password123"
}
```

**POST** `/auth/login`
```json
{
  "email": "john@example.com",
  "password": "password123"
}
```

#### Устройства (требуется авторизация)

**GET** `/devices` - Получить все устройства пользователя

**GET** `/devices/:id` - Получить конкретное устройство

**POST** `/devices` - Создать устройство
```json
{
  "name": "Фикус в гостиной",
  "type": "plant",
  "location": "Гостиная"
}
```

**PUT** `/devices/:id` - Обновить устройство

**DELETE** `/devices/:id` - Удалить устройство

#### Данные датчиков

**GET** `/sensors/:deviceId` - Получить исторические данные

Query параметры:
- `limit` - количество записей (по умолчанию 100)
- `startDate` - начало периода
- `endDate` - конец периода

**GET** `/sensors/:deviceId/latest` - Получить последние данные

### WebSocket события

Подключение:
```javascript
const socket = io('http://localhost:3000', {
  auth: { token: 'your_jwt_token' }
});
```

События:
- `subscribe` - Подписка на устройство
- `sensor-data` - Получение данных датчиков
- `device-control` - Отправка команд управления
- `device-event` - Уведомления от устройства

## 🛠 Разработка

### Добавление нового пользователя

Через API:
```bash
curl -X POST http://localhost:3000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "password123"
  }'
```

### Создание устройства

```bash
curl -X POST http://localhost:3000/api/v1/devices \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "name": "Мое растение",
    "type": "plant",
    "location": "Спальня"
  }'
```

### Доступ к базам данных

#### PostgreSQL (через pgAdmin)
- URL: http://localhost:5050
- Email: `admin@admin.com`
- Password: `dev123`

Параметры подключения к серверу:
- Host: `postgres`
- Port: `5432`
- Database: `neurohome_dev`
- Username: `dev`
- Password: `dev123`

#### MongoDB (через Mongo Express)
- URL: http://localhost:8081
- Базы данных доступны напрямую

### Логирование

Backend логи в реальном времени:
```bash
docker logs -f neurohome-backend
```

### Остановка сервисов

```bash
# Остановить все контейнеры
npm run stop

# Или
docker-compose -f docker-compose.dev.yml down
```

### Очистка данных

```bash
# Удалить все контейнеры и volumes
docker-compose -f docker-compose.dev.yml down -v
```

## 🐛 Troubleshooting

### Проблема: Backend не может подключиться к БД

**Решение**: Убедитесь, что Docker контейнеры запущены:
```bash
docker ps
```

Должны быть запущены: `neurohome-backend`, `neurohome-postgres`, `neurohome-mongodb`, `neurohome-mosquitto`


### Проблема: Mobile приложение не подключается к Backend

**Для Android эмулятора**: Используйте `10.0.2.2` вместо `localhost`

**Для физического устройства**: Используйте IP адрес вашего компьютера:
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

Обновите `packages/mobile/.env`:
```env
EXPO_PUBLIC_API_URL=http://192.168.1.XXX:3000
```

## 📱 Запуск на физическом устройстве

1. Убедитесь, что устройство и компьютер в одной WiFi сети
2. Узнайте IP адрес компьютера
3. Обновите `packages/mobile/.env`:
```env
EXPO_PUBLIC_API_URL=http://YOUR_COMPUTER_IP:3000
```
4. Перезапустите Expo:
```bash
cd packages/mobile
npm start -- --clear
```